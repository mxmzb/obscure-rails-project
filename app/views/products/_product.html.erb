<div id="<%= dom_id product %>" class="scaffold_record">
  <p>
    <strong>Name:</strong>
    <%= product.name %>
  </p>

  <p>
    <%= link_to "Show this product", product %> or <%= link_to "show reviews", product_reviews_path(product) %>
  </p>
</div>
